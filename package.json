{
  "name": "ricecam-admin",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://panzerstadt.github.io/ricecam-admin",
  "dependencies": {
    "axios": "^0.21.1",
    "dayjs": "^1.8.14",
    "file-saver": "^2.0.1",
    "firebase": "^6.0.1",
    "jszip": "^3.2.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "gsutil cors set cors.json gs://operation-verde-ricecam.appspot.com && yarn start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
